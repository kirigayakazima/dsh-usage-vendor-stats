# dsh-usage-vendor-stats bundle patch: when installed as a community plugin layer via dsh plugin add,
# this file inserts the plugin row (host half = package default export; client half = dsh.client marker scan).
- insert:
    - id: usage-vendor-stats
      name: dsh-usage-vendor-stats
